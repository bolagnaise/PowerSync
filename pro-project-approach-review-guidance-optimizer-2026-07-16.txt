ChatGPT Pro Project Approach Review
Generated: 2026-07-16
Project: PowerSync optimizer Discord defects #234 and #266
Conversation: https://chatgpt.com/c/6a581157-46e4-83ec-bf15-ccff62808741

Original task/spec or current user goal:
Confirm and fix two current optimizer defects with focused regressions, preserve
physical constraints and fallback behavior, and release them with the independent
Tesla battery-count fix.

Current approach reviewed:
- #234: exact command modes had stabilized, but raw self-use kW-slot sums drifted
  by about 0.496 per pass and failed an effectively sub-Wh tolerance.
- #266: a reserve-plus-two-percent condition reversed the flat-price objective
  tie-break between rolling solves.
- Proposed fixes were per-run interval-weighted energy comparison and removal of
  the reserve-proximity tie-break while retaining genuine pre-window deadlines.

How to use this guidance:
- Advisory planning context; current repository tests remain authoritative.
- Preserve exact mode equality, per-run comparison, physical constraints, the
  eight-pass bound, and last-feasible-plan fallbacks.
- Do not reintroduce an SOC threshold or unrelated inverter/coordinator changes.

============================================================
First Prompt
============================================================
Review the two evidenced optimizer defects and judge whether the proposed
energy-based convergence comparison and removal of the reserve-proximity
flat-price tie-break are the best scoped fixes. Preserve the full task, existing
physical constraints, genuine SOC deadlines, and fallback behavior. Specify
tests, validation, tradeoffs, and pitfalls.

First Answer
============================================================
Recommendation: adjust, do not pivot.

For #234, keep exact command-mode equality and compare each contiguous self-use
run separately in kWh using the base-slot duration. Use named tolerances:

    MODE_PROJECTION_SELF_USE_REL_TOL = 1e-4
    MODE_PROJECTION_SELF_USE_ABS_TOL_KWH = 0.01

Use math.fsum(power_kw * slot_hours) for each run. Do not compare horizon-wide,
per-slot, modes-only, or terminal-SOC equivalence, and do not change the eight-pass
limit or either projected-plan fallback.

For #266, remove reserve proximity from the global import tie-break. Earlier
flat-price charging should be selected only for an explicit pre-window SOC target.
Do not replace the removed expression with another threshold or hysteresis rule.
Physical reserve and export-floor constraints remain independent of this tiny
objective epsilon.

Add matcher tests for below/above 0.01 kWh, different modes, and separate runs that
cannot cancel. Add a rolling 4.01/4.00/3.99-percent test, a genuine deadline test,
and preserve the infeasible-later-pass fallback regression. Repeat solver-sensitive
cases, run the focused optimizer file, adjacent export guards, Tesla health tests,
and the full suite. Keep the Tesla and optimizer changes independently revertible.

Pitfalls: increasing iterations, suppressing warnings, weakening mode equality,
using a kW tolerance described as energy, scaling tolerance with horizon length,
changing reserve constraints, touching Sigenergy restore logic, or publishing
before focused and adjacent validation.

============================================================
Follow-Up Prompt
============================================================
Give the fastest compliant implementation order, minimum files, commands, checks,
and deferrals while preserving the dirty main worktree through a clean detached
worktree.

Follow-Up Answer
============================================================
Use a clean detached worktree based on the tested Tesla commit. Touch only:

    custom_components/power_sync/optimization/battery_optimizer.py
    tests/test_optimizer_hardware_floor_alignment.py

Then use only RELEASE_NOTES.md and manifest.json for the release slice. Add failing
tests first, implement flat-price stability, then implement per-run energy matching.
Validate the focused tests, repeat degeneracy-sensitive cases three times, run the
adjacent export guard and Tesla suites, then run the full suite once. Verify staged
file lists and keep unrelated dirty files unstaged.

The follow-up reaffirmed that release notes must precede the manifest bump and that
the GitHub release, tag, notification, and automatic notes cleanup must be verified
before reporters are told to update. Runtime hardware replay is unnecessary when
the sanitized HiGHS fixtures reproduce the defects and the focused gates pass.

Local evidence discovered after the review:
The legacy IOG test coupled its real economic/no-export invariant to slot-zero
charging. Independent code review confirmed the slot-zero assertion encoded the
unstable tie-break itself. The assertion was narrowed to charge-within-window while
retaining bounded charge count, charge power, and no-export checks; a below-reserve
case was added to the rolling regression.
