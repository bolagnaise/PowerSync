PowerSync ticket #261 approach review
Date: 2026-07-16

Task
Triage the Sungrow Feed-in Power Limitation report end to end, prove any current-code defect with a focused failing regression, preserve unrelated worktree changes, validate with Python 3.12, release through the manifest workflow, and reply only after publication.

Review prompt 1
Assess whether the current diagnosis and candidate fix are the best continuation: startup persisted recovery restores the saved export-limit baseline, but coordinator.data can still contain the previous temporary target; the next spread-export capture trusts that stale cache and later restores it as though it were the user baseline. Compare cache reconciliation after successful owned restore with forcing a fresh live Modbus read. Preserve restore retry semantics and enabled user baselines.

Review 1 outcome: ADJUST, keep the diagnosis and lifecycle regression

- Put the fix at the ownership boundary in SungrowEnergyCoordinator._restore_captured_export_limit, not only in startup recovery and not in optimizer price logic.
- Reconcile coordinator.data only after both the hardware restore and persisted-state clear succeed.
- For a disabled baseline, set export_limit_enabled=False and leave the raw export_limit_w register value alone because disabling register 13086 does not clear register 13073.
- For an enabled baseline, set export_limit_enabled=True and export_limit_w to the restored user value.
- Do not update cache or release captured ownership on either hardware or Store failure; current retry state must remain intact.
- Do not force every capture through get_battery_data. It is a broad multi-register read and incomplete reads would require a larger tri-state/refusal design.
- The regression must cross persisted startup recovery, an immediate second spread-export cycle, intermediate persisted baseline capture, and final restore.
- Parameterize the concrete disabled baseline and an enabled 5000 W user baseline. Expected write sequences are [None, 4400, None] and [5000, 4400, 5000].
- Validate the full Sungrow file, then adjacent force-mode, curtailment, restore-retry, superseded-restore, and unload coverage.

Review prompt 2
Give the fastest compliant implementation and release sequence without reducing scope: exact edit order, minimum regression rows and adjacent tests, release verification, and final guardrails.

Review 2 outcome: fail-first regression, one production hunk, two commits, one push, publication gate

1. Record branch, origin, staged state, and unrelated worktree changes; isolate only the target hunks.
2. Add the parameterized lifecycle regression first and confirm it fails on the recaptured stale baseline.
3. Add only the successful ownership-to-cache handoff in _restore_captured_export_limit.
4. Run tests/test_sungrow_sh_controller.py and the adjacent restore-contract files with python3.12; run diff checks.
5. Commit the fix separately from RELEASE_NOTES.md plus the manifest patch bump.
6. Push main once, watch Create Release on Version Bump, verify the published non-draft release, tagged manifest, Discord notification marker, cleared release notes, and cleanup commit.
7. Reply to the reporter only after those publication checks pass.

How this guided the work
The implementation used the five-line cache reconciliation exactly inside the existing limit_ok and persisted_ok success block. The regression covers both disabled and enabled baselines, and the unrelated BatteryControllerWrapper false-return masking was deliberately left out because it is a separate hardening gap rather than the cause of this successful-recovery race.
