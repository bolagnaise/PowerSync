ChatGPT Pro Project Approach Review
Generated: 2026-07-16
Project: PowerSync (/Users/benboller/Developer/power-sync)

Original task/spec or current user goal:
Fix the current v2.12.863 monitoring-mode reload defect so ordinary reload/shutdown issues no hardware commands, while a deliberate false-to-true monitoring transition still releases prior PowerSync control across Tesla and Modbus brands. Add focused regressions and preserve the release workflow.

Current approach reviewed:
Inference from current code and the reporter's exact log: make OptimizationCoordinator.disable() stop its executor with restore_normal=not monitoring_mode, and move all sanctioned cleanup to explicit monitoring-enable transitions. Preserve BatteryControllerWrapper's monitoring restore bypass for those explicit cleanup calls.

How to use this guidance:
- Use as advisory planning context.
- Verify claims against local files and tests.
- Do not reduce scope.
- Prefer the follow-up answer where it corrects the first answer using newer local evidence.

============================================================
First Prompt
============================================================
Review the proven Tesla Powerwall monitoring-mode reload sequence, the unconditional executor restore in OptimizationCoordinator.disable(), the explicit ProviderConfigView force cleanup, and the choice between conditional shutdown restore versus new ownership tracking. Preserve cross-brand behavior and require focused Python 3.12 regressions.

First Answer
============================================================
Recommendation: KEEP with a small adjustment.

The review agreed that the core change should be:

    await self._executor.stop(restore_normal=not monitoring_mode)

It said this directly prevents hardware writes during ordinary monitoring reload/shutdown and that _allow_monitoring_restore=True must remain available for explicit cleanup paths. It initially recommended a minimal active-optimizer-state exception for stale ownership, with tests for monitoring reload, active force cleanup, explicit monitoring enable, non-monitoring shutdown, and Tesla/Modbus behavior.

The answer also warned not to move general restore behavior into providers or remove the explicit monitoring bypass.

============================================================
Follow-Up Prompt
============================================================
Reconcile newer verified evidence: ScheduleExecutor has no cheap complete ownership signal; _get_active_force_state() misses pre-IDLE reserve and native/tariff state; ProviderConfigView is not the only enable path; MonitoringModeSwitch updates before cleanup; the HA options flow force-cleans only Sigenergy; ordinary already-monitoring reload must issue zero writes; deliberate false-to-true handoff must clean all prior PowerSync state. Choose A (skip all monitoring shutdown restore and harmonize explicit handoffs), B (partial force-state exception), or C (new ownership tracking).

Follow-Up Answer
============================================================
Direct choice: A.

The review concluded that partial ownership checks are incomplete and new executor ownership tracking is too broad. Its guiding invariant was:

- Ordinary lifecycle while monitoring is already active: zero hardware writes.
- Deliberate false-to-true monitoring enable: explicit cleanup before the option flip.

Recommended slice:

1. Change OptimizationCoordinator.disable() to stop the executor with restore_normal=not monitoring_mode.
2. Move MonitoringModeSwitch cleanup before async_update_entry and make an already-on request a no-op.
3. Share the existing explicit cleanup behavior rather than invent ownership tracking.
4. Apply the false-to-true cleanup consistently to the switch, ProviderConfigView, and the HA options flow.
5. Test monitoring reload, cleanup ordering, options-flow transition, ProviderConfigView consistency, already-true no-op, and non-monitoring restore.

The review proposed implementing the shared cleanup via coordinator.disable() plus restore_normal. Local reconciliation rejected that detail because disable() already restores through the executor and would double-fire the service cleanup; it can also leave the no-reload API path's coordinator disabled. The verified local primitive is instead the existing ProviderConfigView sequence: SERVICE_RESTORE_NORMAL with _force_restore=True, followed by _restore_pre_idle_backup_reserve(..., bypass_monitoring=True) when pending.

Deferred after this slice: formal ownership tracking or a broader monitoring handoff API. Neither is needed for the proven defect.
