<!-- release: v2.12.830 -->

## What's Changed

**Show home load separately during battery export**
The LP Battery Power forecast now splits battery discharge into the portion powering the home and the portion exported to the grid. This makes the chart match the optimizer's existing SOC and power-balance calculations during export windows instead of displaying only one combined discharge series.

Update available via HACS
