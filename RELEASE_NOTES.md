## What's Changed

- Dashboard: keep the grid section faintly visible when idle on darker rainy and night scene variants.
- This fixes cases where the grid node looked missing on `scene_day_rain_idle.png` even though the scene asset and layout were present.

Update available via HACS
