<!-- release: v2.12.844 -->

## What's Changed

**Spread Export now uses the full eligible window**

Fixed Smart Optimization filtering later export slots using the concentrated plan's already-depleted SOC at the reserve floor. With Spread Export enabled, planned export energy is now flattened across the remaining eligible tariff window while preserving charge slots, home-load supply, the configured export cap, and the optimizer reserve floor.

Update available via HACS
