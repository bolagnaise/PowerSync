<!-- release: v2.12.860 -->

## What's Changed

**CovaU entries keep the correct provider name**
PowerSync now keeps an explicitly configured CovaU SolarMax entry labelled as CovaU when AEMO spike detection is also enabled, instead of the legacy AEMO-only fallback renaming it to GloBird during startup.

Update available via HACS
