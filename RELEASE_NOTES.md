<!-- release: v2.12.869 -->

## What's Changed

**Sungrow feed-in limits now return to the saved setting after recovery**
PowerSync no longer re-captures a stale temporary spread-export target after recovering an interrupted Sungrow export window. Systems whose normal Feed-in Power Limitation was disabled remain disabled after later export cycles, while enabled user limits continue to be restored.

Update available via HACS
